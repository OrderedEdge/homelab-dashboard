[project]
name = "homelab-dashboard"
version = "5.0.0"
description = "Showcase homelab infrastructure dashboard"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["backend/tests"]
